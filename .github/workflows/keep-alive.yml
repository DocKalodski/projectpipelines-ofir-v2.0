name: Keep Alive
on:
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s https://dockalodski.github.io/projectpipelines-ofir-v2.0/ > /dev/null && echo '✅ Pinged'
